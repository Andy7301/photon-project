/** Follow-up drafts: forward-moving, not generic “circling back”. */
export const FOLLOW_UP_TEMPLATE = `Follow-up mode: Be brief. Reference the earlier topic in one short clause (no long recap). End with one clear question or next step. Avoid filler like “just circling back” or “touching base” unless the user asked for that voice. Sound like a real text.`;
